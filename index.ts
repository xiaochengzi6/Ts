// ---------------------------- 阅读须知 ----------------------------

// 文档都是在 ts 文件中写的 大家可以按照 readme.md 中的目录来看

// 每个文件开头都有目录 可以通过 vscode/浏览器 中的查找 Ctrl + F 键入目录的关键词定位到此处

/**
 * tips1:
 *
 * keyof 是索引查询 针对[对象、class]这两者是索引类型
 * 索引可以做出变化，用 as 操作符，叫重映射
 */

/**
 * tips2:
 *
 * ts 支持三种可以声明任意类型的变量 1.type、 2.infer、 3.类型参数[ 比如 type Value<T> 中的 T ]
 */

// 最后 欢迎大家提出问题 issue 或者 共建
